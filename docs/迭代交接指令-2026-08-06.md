# Meridian 迭代工作交接指令（2026-08-06）

> 角色：FrontendDeveloper / FullstackDeveloper · 执行环境：**新工作空间（指向 F:\Meridian）**
> 产品状态：已上线（Vercel）· 数据库 Neon · git 仓库 github.com/Pan-An331/Meridian
> 必读：`README.md`（项目导航）→ `Task OS 项目总控文档 V2.0.md`（产品宪法）→ `docs/任务信息架构规范-V3.md`（开发 SSOT）→ `AGENTS.md`（纪律）

---

## 〇、开工前环境确认

```bash
cd F:\Meridian
npm install        # 首次（约 1-2 分钟）
npm test           # 基线 95/95 必须全过
npx tsc --noEmit   # 零错误
```

- `.env` 已配置 Neon 直连串（DATABASE_URL）+ AUTH_SECRET + AUTH_TRUST_HOST
- 部署循环：**git commit → git push origin main → Vercel 自动重新部署**（无需手动）

---

## 一、本次任务范围（对照 docs/产品改进清单-2026-08-06.md v2）

只做以下 4 项（**P1-6 / P1-4 / P1-11 / P1-2**），其余项待后续批次：

### A · P1-6 Project 待整理池项可拖拽（明确 bug）
**文件**：`src/app/(dashboard)/projects/page.tsx`
**现状**：树节点有 `draggable`（384 行附近），待整理池（poolList）项**无 draggable/onDragStart** → 拖不动。
**要求**：
- 给待整理池项补 `draggable` + `onDragStart`（`setDragId(id); setDragSource("pool")`），复用现有 drop 逻辑（pool → 拖到树 = 挂为子级）
- 待整理池项 hover 视觉提示"可拖拽"
- **验收**：待整理池项能拖入左侧树成为子级，toast「已挂入 XX」；树中已存在该任务后池中消失

### B · P1-4 Inbox 新建项目/子项目输入框可取消（明确 bug）
**文件**：`src/app/(dashboard)/inbox/page.tsx`
**现状**：输入框 Escape 只 `setValue("")` 清空值，**无法关闭输入框**（56 行附近）。
**要求**：
- Escape → 关闭输入框（不只是清空）
- 输入框旁加「✕」取消按钮
- 失焦（blur）关闭
- **验收**：任意时刻能取消新建，不强制完成

### C · P1-11 Focus Card 执行清单可新增项（明确 bug）
**文件**：`src/components/today/FocusCardV2.tsx` + 清单数据 API
**现状**：MemoList 只有渲染 + `onItemToggle`（勾选），**加号/备注无 onItemAdd 回调** → 点了没反应。
**要求**：
- 补 `onItemAdd?(title: string)` 回调（今日页接线）
- 加号展开 inline 输入 → 回车/确定提交 → 调现有 memo 写入 API（任务子项创建）
- 新增项立即出现在清单里（乐观更新）
- **验收**：清单型任务点加号能添加子项，刷新后保留

### D · P1-2 Today 卡片「预计/已用 0 分钟」显示修复
**文件**：`src/app/(dashboard)/today/page.tsx`
**现状**：无排期任务 `plannedMinutes=0` → 显示"预计 0 分钟"，观感差。
**要求**：
- 无排期：不显示"预计 X 分钟"，改"待排期"（弱化灰字）
- 已用时间为 0 且未开始：显示"—"而非"0 分钟"
- 核查"闲/空闲"文案（plan/page.tsx:311 周历用，确认未误入 Today）
- **验收**：未排期任务卡片不再出现"0 分钟"

---

## 二、工程纪律（红线）

- 每阶段：`tsc --noEmit` + `npm test`（95 基线不回归）+ 完成后 `next build`
- **禁止**：新增数据模型/API 路由（本批 4 项全是前端/现有 API 消费）；改动 Focus Card 状态机（只加回调）；动 Today 规则引擎
- 改 UI 对照 `UI示例/` 定稿稿（FocusCard-V2-UI副本.html / Project-优化副本-2026-08-04.html）
- 写实施简报到 `docs/`（命名：`XXX-实施简报-2026-08-06.md`），完成后归档至 `docs/archive/`

---

## 三、完成后交付

1. 每项标记"✅ 验收标准达成"到实施简报
2. `git add -A && git commit && git push origin main`（Vercel 自动部署）
3. 通知产品本人验收（抽查代码 + 线上实测）

---

## 四、后续批次（本次不做，记录在案）

- P1-1 今日路线提前执行
- P1-9 自定义主题"确定即保存"（UX 简化）
- P1-10 预估单位扩展（分钟/小时/天）
- P1-5 形态意图入口（需产品决策 D-新2 确认）
- P2-7/P2-8/P1-12 视觉打磨（Project 字号、Focus Card 等比例放大）

*交接指令 · 2026-08-06 · 项目路径 F:\Meridian（新工作空间）*
