# V3 后端实施报告（2026-08-03 · 阶段 A→D 全部完成）

> 依据：《V3实施-开发分工指令.md》§2 后端任务 + 《任务信息架构规范 V3》§4 后端开发契约
> 范围：数据库 → 服务层 → API → AI 链路，四阶段顺序执行，每阶段 tsc 验证通过
> 状态：**全部完成 ✅**（tsc ✅ / vitest 59/59 ✅ / next build ✅ / dev 冒烟 ✅）

---

## 阶段 A：数据库（✅）

### A1. schema 变更
`prisma/schema.prisma` Task 模型：
- **删除 5 个死字段**：`temperature` / `startTime` / `endTime` / `cognitiveLoad` / `schedulingHint`（决策 D12）
- **新增 `theme String?`**（决策 D9，主题独立字段）
- **新增 `@@index([userId, theme])`**（主题统计/过滤索引）

### A2. 迁移脚本（新增 `scripts/v3-theme-migration.ts`，可重复执行）
| 步骤 | 结果 |
|------|------|
| competition → practice + theme:竞赛（D8） | 1 条 |
| learning 标题含「考研」→ theme:考研（**保守规则**：仅明确「考研」语义，不因"数学/英语/政治"单独出现误判，对齐审查结论 §1.5.5） | 1 条 |
| 剥离 tags 中 domain:/theme:/important: 前缀（theme:* 回填 theme 字段） | 17 条 |
| 校验：competition 残留 / 系统前缀残留 | **0 / 0** ✅ |

> 注意：V3 §4.2 迁移步骤 3 原文是「标题含考研/数学/英语/政治/专业课 → 考研」。审查结论 §1.5.5 明确要求保守（"学习PLC"不可误判考研）。**两者冲突时取保守规则**（更符合产品"不强猜"红线），已在脚本注释说明。如产品确认要激进规则可改脚本重跑。

### A3. 执行方式
- 环境坑：`prisma migrate dev` 在非交互终端不可用 → 改用 **`prisma db push --accept-data-loss`**（先备份 `prisma/dev.db.bak-v3`）+ `prisma generate`（总控 §6.5 规定的工作流）
- 改 schema 前已杀 dev server（db 锁）
- temperature 45 个存量值是 importance 派生的冗余数据，删除无信息损失

---

## 阶段 B：服务层（✅ tsc 零错）

| 文件 | 改动 |
|------|------|
| `src/lib/plan/colors.ts` | 删 `buildTagsForDomain`（零引用确认）；TAG_MAP 移除「英语」→learning（V3 B1；DOMAINS/THEMES/resolveTheme 前端先行步已就绪，后端复用） |
| `src/lib/ai/parser.ts` | INBOX_ANALYZER_PROMPT 加 theme 字段 + **competition 分类并入 practice（D8）**；CATEGORY_RULES 删 competition、加 THEME_RULES + matchTheme fallback；items 映射 +theme（LLM 明确值优先，fallback 关键词兜底） |
| `src/lib/inbox/task-builder.ts` | TaskCreateParams 删 temperature/cognitiveLoad；**+theme 透传**（单任务/积累型/四层建树全链路） |
| `src/lib/inbox/confirm-service.ts` | **category 白名单 7 类**（归一化 + other→null，与其他入口存储一致）+ theme 白名单（≤20 字）；**D2 反馈回流**：AI 推断值 ≠ 用户值 → AgentFeedback(modifiedField=category/theme) |
| **新增 `src/lib/task/theme.ts`** | getTheme（字段优先→推断兜底）/ themeColor（复用 colors）/ normalizeThemeInput（≤20 字/去空白/null 清除） |
| `src/lib/ai/executor.ts` | 删 temperature/cognitiveLoad/schedulingHint 写入；+theme 透传（AI create_task） |
| `src/lib/ai/today-decision.ts` | cognitiveLoad 判定改用 complexity（D12 删字段后的唯一保留替代） |
| `src/lib/ai/planner.ts` | 删 cognitiveLoad/schedulingHint 读取；+theme 字段注入 + 主题分布提示 |

---

## 阶段 C：API（✅ tsc 零错 + 登录态冒烟全通过）

### C1. 新增 `GET /api/tasks/search` ⭐
- 标题/标签 contains + **归属链**（祖先标题命中 → 返回其子孙任务两级）
- 结果：`{ results: [{id,title,category,theme,status,parentTitle,scheduledStart?,deadline,tags}] }`
- 空 q → 400；批量取排期防 N+1
- **冒烟发现并修复 1 个 bug**：祖先自身命中时被排除导致归属链失效 → 修复后「搜"考研项目"」能同时返回父任务 + 子任务（带 parentTitle）

### C2/C3. tasks POST/PUT +theme
- POST：+theme 白名单（≤20 字）；删 temperature 写入
- PUT：+theme 白名单（null 清除）；**+D2 回流**（category/theme 变更 → AgentFeedback context=archive_panel）；删 temperature 残留写入（Record<string,unknown> 逃过类型检查但运行时 Prisma 会拒收，已修）

### C4. inbox/analyze items +theme
parser 已返回 → route 透传（冒烟验证：背单词→theme:考研/learning，电赛→theme:竞赛/practice ✅）

### C5. views/today
currentTask +theme/themeColor（两个查询点：in_progress 整行 + 时段排期 select 补 theme）

### C6. views/week-calendar
scheduledTasks/plannedTasks +theme/themeColor；**响应 +themes 图例**（name/count/color/deep/bg）

### C7. tasks/[id] 档案聚合 ⭐
`+ancestors`（归属链标题数组）/ `+schedules` / `+accumStats`（积累型）/ `+aiFields`（只读，AI 增强红线）

### C8. views/stats ⭐
- **+themeBreakdown**（决策 D3/D17：按排期任务数聚合 → `[{theme,count,percent,label:主攻|待加强}]`，非时长）
- **+metrics 指标卡**（决策 D2）：`completionRate / adoptionRate / backlogRate / checkinKeepRate`
- **+?theme= 过滤**（completedTasks）

### C9. 死路由删除（4 个，grep 零引用确认）
`/api/ai/ingest`、`/api/ai/today-suggest`、`/api/plan/edit-schedule`、`/api/plan/delete-schedule`（`/api/views/week` 已不存在；**保留** `/api/plan/repeat`）

---

## 阶段 D：AI 链路（✅ tsc 零错）

| # | 改动 |
|---|------|
| D1 | buildAgentContext 注入 **themeDistribution**（"用户当前主题：考研×N"）；planner prompt 同步注入 |
| D2 | 反馈回流**双点**：confirm-service（inbox_confirm）+ PUT tasks/[id]（archive_panel），写 AgentFeedback(modifiedField=category/theme, originalValue, userValue, taskId) |
| D3 | pattern-mining **Rule 10「主题时段偏好」**（theme_time_*：某主题排期 ≥3 次被移且某时段占比 ≥60%） |

---

## 验证结果

| 验证 | 结果 |
|------|------|
| `tsc --noEmit` | ✅ 零错误（含清理 .next/.next-prod 缓存后） |
| `vitest run tests/` | ✅ **59/59**（新增 tests/v3-theme.test.ts 9 用例：normalizeThemeInput/getTheme/themeColor） |
| `next build` | ✅ 通过（17s，/api/tasks/search 已纳入构建） |
| dev 冒烟（登录态） | ✅ 页面 6 个 200/307；注册→CSRF→登录→session 全流程通 |
| API 冒烟 | ✅ search（空q 400/关键词/归属链）、POST+theme、档案聚合、PUT theme+回流、week-calendar themes、stats themeBreakdown+metrics、today、inbox analyze |
| 冒烟数据清理 | ✅ 测试账号与全部关联数据已删除（外键逐表清理） |

---

## 交接说明（前端第 3 步需处理）

| 前端现状 | 对接动作 |
|---------|---------|
| **TaskArchivePanel.save() 未发 theme 字段**（简报声称"前端无需改"不成立） | 前端在 save() 的 body 补 `theme`（一行）：`...(theme ? { theme } : { theme: null })` |
| Review ThemeInvestment 按专注时长聚合，**与 D17（按任务数）口径不符** | 前端改消费 `stats.themeBreakdown`（含 percent/label 主攻/待加强） |
| Review MetricCards 采纳率/堆积率/打卡保持率占位"—" | 前端消费 `stats.metrics.{adoptionRate,backlogRate,checkinKeepRate}` |
| GlobalSearch 用 resolveTheme 推断主题 | 后端已返回 h.theme，可改直读（可选优化） |
| 档案面板归属链前端自己拼 | 后端已返回 ancestors，可改直读（可选优化） |

## 遗留
- `.trash-v3/` 临时目录（safe-delete 机制拒绝删除已移出的死路由/临时脚本，确认无问题后可手动清理）
- `prisma/dev.db.bak-v3` 迁移前备份（确认稳定后删除）
- D3 Rule10 为可选增强已落地，未做专门单测（依赖真实观察数据）
- 导出 JSON 目前仅含聚合视图（stats/memories/week），不含原始 Task 明细——如需"全量可迁移备份"（任务表+排期+日志原始 JSON）属更大改动，可作为独立后续项

---

## 补充：D5 数据主权（2026-08-03 补，用户审查发现遗漏）

> 总控 R4 红线：导出 JSON 必须带 `schemaVersion` + 迁移映射表。原导出无版本标记 → 补。

- **新增 `src/lib/export-version.ts`**：`EXPORT_SCHEMA_VERSION = 2` + `EXPORT_MIGRATION_MAP`（v1→v2 变化：competition→practice+主题竞赛 D8 / theme 字段 D9 / 死字段删除 D12 / tags 前缀剥离）+ `buildExportHeader()`
- **settings 导出接入**：`exportJson()` payload 改为 `{ ...buildExportHeader(exportedAt), stats, memories, week }`，版本信息位于 JSON 顶层
- 语义：**v1 = V2 时期格式（2026-08-02 前）；v2 = V3 当前格式**
- 验证：tsc ✅ / vitest 63/63 ✅（新增 tests/v3-export-version.test.ts 4 用例）/ next build ✅

*阶段报告 · V3 后端四阶段完成（+D5 补丁）。下一步：前端对接真实数据（分工指令第 3 步）。*
