# 收尾批次 · 后端实施简报（2026-08-04）

> 依据：《收尾批次-后端开发指令-2026-08-04.md》
> 状态：**完成 ✅**（tsc ✅ / vitest 95/95 ✅ / API 冒烟通过 ✅）

---

## 改动说明

### 现状核查（重要发现）

**`monthDates` 已在 accum.ts 返回**——A1 所要求的字段在此前批次已实现：
- `src/lib/task/accum.ts`：`AccumStats` 接口含 `monthDates: string[]`（第 16 行）+ 计算逻辑（第 57 行）+ 返回（第 67 行）✅
- `src/app/api/views/today/route.ts`：`card.accumStats = await getAccumStats(...)` 整体透传，monthDates 自动带出 ✅

### 本轮实际改动

| # | 文件 | 改动 |
|---|------|------|
| A1' | `src/lib/task/accum.ts` | 把 monthDates 过滤逻辑抽成**纯函数 `filterMonthDates(dates, now)`**（保持原逻辑不变，只提取复用——与 streak 的 computeStreak 同模式，使逻辑可单测） |
| A2' | `views/today/route.ts` | **零改动**（已整体透传，确认即可） |
| A3 | `tests/accum-month-dates.test.ts`（新增） | 3 用例：混合当月/上月/下月 → 只取当月升序去重 / 无当月 → 空数组 / 跨月边界正确归属 |

> 指令 Non-goal"不改 monthDates 计算逻辑"——`filterMonthDates` 与原逻辑逐字等价（同前缀匹配 + 升序去重），仅重构提取。

---

## 验证结果

| 项 | 结果 |
|----|------|
| `tsc --noEmit` | ✅ 零错误 |
| vitest | ✅ **95/95**（92 基线 + 3 新增） |
| API 冒烟 | ✅ `GET /api/views/today` → 积累型 currentTask.accumStats.monthDates 正确 |

### 冒烟实测

造数据：积累型任务「背单词」+ checkin TimeLog 5 条（上月 2 条 7/20、7/25 + 当月 3 条 8/2、8/5、8/5 重复）+ 今日排期。

```
currentTask: 背单词
accumStats.monthDates: ["2026-08-02","2026-08-05"]
✅ 只含当月(2026-08): true | 升序: true | 去重: true | 当月 2 条: true
```

- ✅ 上月打卡（7/20、7/25）被过滤
- ✅ 当月 2 条（8/2、8/5）升序返回
- ✅ 8/5 重复打卡去重只留 1 条
- ✅ views/today 透传正常（currentTask 判定命中今日排期）

---

## 前端衔接

前端积累·每日卡「当月小日历」（mini-cal 迁 V2）消费 `currentTask.accumStats.monthDates`：
- `monthDates: string[]` → 当月哪些天打了卡（YYYY-MM-DD 升序）
- 与 `monthCount`（本月打卡数）、`monthTotalDays`（本月总天数）配合渲染小日历
- 前端类型定义 `today/page.tsx` 已声明 `monthDates`（第 29 行）✅，仅需在 toCardV2 透传给 FocusCardV2 消费（前端指令阶段 D 范围）

---

## 交付物

- [x] `accum.ts`：filterMonthDates 纯函数 + monthDates 透传确认
- [x] `tests/accum-month-dates.test.ts`：3 用例
- [x] tsc / vitest 95/95 / API 冒烟
- [x] 冒烟数据清理（45 tasks / 4 users 与基线一致）

*实施简报 · 收尾批次后端唯一改动完成，mini-cal 前端可消费。*
