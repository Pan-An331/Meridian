# API 层 ↔ 服务层交互审计报告（2026-08-02 第八轮）

> 范围：49 个 API 路由与核心服务层的契约质量（绕过、校验、错误格式、鉴权、一致性）
> 验证：tsc 零错误 / vitest 29/29 / build 成功

---

## 一、总览

| 维度 | 结果 |
|---|---|
| API 路由 | 49 个（auth 2 个公开） |
| 鉴权覆盖 | ✅ 47/47 全部有 session 检查（零遗漏） |
| 错误返回形态 | 修复前 5 种并存 → 已收敛主流为 `{ error: string }` |
| 时间源绕过 | 5 处 → 已收口（shiftSchedules 服务化） |

## 二、P0 修复（2 项）

**1. agent/memory/dashboard 记忆操作 IDOR**（block/pin/unblock 凭 memoryId 直接改，无归属校验 → 可篡改他人记忆）
✅ 操作前 `findFirst({ id, userId })` 校验，未命中 404。

**2. postpone 延期天数无类型校验**（`"abc"` 通过 `"abc" < 1` 判断 → NaN 偏移写库）
✅ `Number.isInteger && 1-365` 白名单 + 边界。

## 三、P1 修复（12 项）

| # | 问题 | 修复 |
|---|---|---|
| 3 | delete-schedule 绕过 service 直写 | this 走 `deleteScheduleById`（带归属） |
| 4 | **edit-schedule 时间修改绕过 service**（updateSchedule 硬约束"必须走 moveSchedule"被整段绕开） | 新增 service `shiftSchedules()`（tx 可选注入），all/future 收口 |
| 5 | **views/week 查死字段 Task.startTime**（全库无写入点 → 周视图事件恒空） | 改查 Schedule 表 |
| 6 | schedule PUT 无日期校验（POST 有） | 对齐 isNaN 校验 |
| 7 | action adjust_time 无日期校验 + 整个 handler 无 try/catch | isNaN + end>start 校验 |
| 8 | ai/today-decide 无 try/catch（抛 HTML 500） | 已标注（前端未接，接线前补） |
| 9 | apply-decision 批量不校验日期 + 部分失败返回 200 | 预校验全部日期 + 部分失败 422 |
| 10 | plan/repeat slots 无格式校验 | 已标注 |
| 11 | ai/ingest 多步写无事务 + LLM 时间未校验 | 已标注（ingest 无人调用，接线时一并修） |
| 13 | **agent/state 直写 userState 绕过白名单**（任意 stateType 可写入污染决策） | stateType 白名单 |
| 15 | **错误格式 5 种并存**（前端需写 4 套分支） | decision/confirm、agent/confirm 收敛为 `{ error }` 主流格式 |
| 16 | tasks PUT importance clamp 而非校验（"abc"→NaN 误导） | typeof + 整数 + 1-5 校验 |
| 17 | agent/feedback userResponse 无白名单（污染 analyzeFeedback 规则） | accepted/modified/rejected/ignored 白名单 |

## 四、P2 规范项（已标注）

- DELETE 返回 200 而非 204（2 处）
- plan/analyze、plan/decision 纯读用 POST（应 GET）
- 参数命名三套（scheduledStart/newStart/startTime）
- schedule GET 不去重（管理接口）
- agent/chat auto-planner 直查 prisma（口径漂移风险）

## 五、架构级修复（Task 29）

1. **Schedule Service 新增 `shiftSchedules(userId, taskId, offsetMs, { scope, refStart, tx })`** — 支持 tx 注入，批量平移收口服务层（edit-schedule 的 all/future 复用）
2. **planner saveSchedule N+1 消除**：预取用户全部排期到内存判断（原来每条 2 次 findFirst），同时补日期 isNaN 校验

## 六、遗留（标注，未做）

1. ai/ingest 多步无事务 + 时间未校验（该路由零调用，接线时统一修）
2. ai/today-decide 无 try/catch（前端未接）
3. plan/repeat slots 格式校验
4. 错误格式全量统一为 `{ success, error: { code, message } }`（当前收敛为 `{ error }` 主流，前端已兼容）
5. 死路由群清理（plan/{week,day,move,repeat}、schedule、timer、exec-state、decision/*、ai/*）——建议下一轮"接线或删除"决策

## 七、审计方法论

- **服务层旁路检测**：grep 所有 prisma 写操作 vs service 导出函数 → 找出绕过点
- **校验覆盖矩阵**：每个 POST/PUT 路由检查（类型、isNaN、枚举、边界、try/catch）
- **错误格式审计**：收集所有错误返回 → 统计形态 → 收敛
- **契约审计**：service 返回结构 vs API 使用 vs 前端期望三方对齐
