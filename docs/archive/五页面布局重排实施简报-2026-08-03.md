# 五页面布局重排 · 实施简报（2026-08-03）

> 依据：《docs/五页面布局重排方案-2026-08-03.md》· 只动布局/折叠，**未动数据、接口、功能逻辑** · 三区原则：核心行动首屏 → 辅助分析靠后 → 自我汇报沉底折叠

---

## 1. Today · 今日驾驶舱

**改动**：状态仪表盘从「Focus Card 与今日路线之间」移到**页面底部沉底折叠条**（方案 §1）
- 折叠条标题「今日状态」+ emoji 摘要 + 调整按钮 + 箭头，**默认收起**
- 展开：4 项状态展示 + 内联「调整」编辑区（原 StatusBar 逻辑原样，仅包折叠）
- 页面顺序：页头 → Focus Card V2 → 今日路线|AI 助手（双栏）→ **今日状态（沉底折叠）**

**顺手项 2 ✅ 死代码清理**（-26KB）：删除 FocusCardOneCol / FocusCardTwoCol / AccumPanel / TimerRing / FocusCardV2Demo / typeMeta / progColor / tagMap / btnGhost / isOneCol / aiExecText / layout 状态（页面已纯 V2，tsc 确认零引用后删）

## 2. Plan · 规划

**改动**（方案 §2，采纳"截止比收集箱紧急更靠上"）：
- **本周截止** → 顶部折叠条（默认收起，露 badge「N 个截止 · M 个紧急」，紧急时红底）
- **周历甘特图**保持全展开（核心）
- **收集箱**贴周历（拖拽源，位置不变）
- **计划健康度** → 沉底折叠条（默认收起，badge 显示分数；展开显示进度条 + 全部 issues）
- 副标题弱化

## 3. Review · 复盘（手术重点）

**顺序重排**（方案 §3）：`战报（故事）→ 指标卡（健康仪表）→ 主题投入 → 产出日记（证据）→ 本周洞察（精简）→ 下周建议（行动）`

| 改动 | 结果 |
|------|------|
| ① 战报删「连续天数」 | 本周基础 3 格 → 2 格（完成任务 / 专注时长），连续天数与指标卡打卡保持率重复 |
| ② 本周洞察删 2 保留 2 | 删「分类时间占比条」「最认真时段」；保留「任务时段偏好」「打断注意」（战报已表达结论） |
| ③ 主题投入加周环比趋势 | 每行加「较上周」badge：上升绿 ↑ / 下降红 ↓ / 持平灰 —；**后端未就绪显示"—"**（themeBreakdown 项预留 `prev?: number|null` 可选字段，后端就绪自动填充） |
| 副标题弱化 | ✅ |

## 4. 顺手项

| # | 项 | 结果 |
|---|----|------|
| 1 | 页头副标题弱化 | ✅ Inbox/Plan/Review 说明性副标题改 `text-xs /70`（Today 问候语保留） |
| 2 | Today 死代码清理 | ✅（见 §1） |
| 3 | Project poolTasks 死代码 | ✅ 删除（实际用 poolList） |
| 4 | Project 提示横幅可关闭 | ✅ ✕ 按钮 + `localStorage("taskos.projects.hint.dismissed")` 记忆，刷新不再显示 |

## 5. 验证

| 项 | 结果 |
|----|------|
| `tsc --noEmit` | ✅ |
| `vitest run tests/` | ✅ **73/73**（方案预期 73 用例不受影响） |
| `next build` | ✅ |

## 6. 明确未做（Non-goals 遵守）
- 未加任何新卡片、未动数据模型/API 契约、未改 Inbox/Project 主体布局、未做状态仪表盘样式重设计、未动 Review 下周建议逻辑
- 唯一预留：themeBreakdown `prev` 可选字段（前端已兼容 null → 显示"—"，后端就绪自动生效）

---

*实施简报 · 五页面布局重排完成，交产品验收。*
