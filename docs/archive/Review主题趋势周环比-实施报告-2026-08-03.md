# Review 主题趋势 · 周环比数据实施报告（2026-08-03）

> 依据：《Today弹性与Review两栏-开发分工指令-2026-08-03.md》§2 后端工程师任务
> 状态：**完成 ✅**（tsc ✅ / vitest 79/79 ✅ / API 冒烟 ✅）

---

## 改动内容

### 1. 纯函数抽取（`src/lib/task/theme.ts` 追加，可单测）

| 函数 | 说明 |
|------|------|
| `aggregateThemeCounts(scheduled)` | 单周期主题聚合：按**排期任务数** count（决策 D3/D17 口径，非时长）；无主题任务不计入；返回 `{ byTheme, total }` |
| `buildThemePrev(theme, currentTotal, prev)` | 周环比 prev 构建：上周有该主题 → `{count, percent}`（percent = 上周 count / 上周 total）；上周无该主题 → `null` |

### 2. stats route（`src/app/api/views/stats/route.ts`）

- 增加**上周周期查询**：`scheduledStart ∈ [prevPeriodStart, periodStart)`（prevPeriodStart = periodStart − days），与本周窗口衔接、口径一致
- `themeBreakdown` 每项加 `prev: { count, percent } | null`：
  - 本周与上周查询 `Promise.all` 并行，无 N+1
  - **向后兼容**：现有字段 `theme/count/percent/label` 语义不动；`weekOverWeek` 不动；缺省 `null`（前端显示"—"）
- 其他接口零变更

---

## 验证结果

| 验证 | 结果 |
|------|------|
| `tsc --noEmit` | ✅ 零错误 |
| `vitest run tests/` | ✅ **79/79**（新增 `tests/theme-breakdown-prev.test.ts` 6 用例：聚合口径 / 有上周数据 / 无上周数据 → null / 空数据 / percent 兜底） |
| API 冒烟 | ✅ `GET /api/views/stats` 返回 `themeBreakdown[].prev`，三种情形全验证 |

### API 冒烟实测（造数据：上周考研×2+竞赛×1，本周考研×2+身材×1）

```json
[
  { "theme": "考研", "count": 4, "percent": 67, "prev": { "count": 1, "percent": 100 }, "label": "主攻" },
  { "theme": "竞赛", "count": 1, "percent": 17, "prev": null, "label": "待加强" },
  { "theme": "身材", "count": 1, "percent": 17, "prev": null, "label": "待加强" }
]
```

- ✅ **有上周数据**：考研 prev 返回上周 count/percent（口径一致按任务数）
- ✅ **无上周数据**：竞赛/身材 prev 为 `null`
- ✅ 旧字段（theme/count/percent/label）未动，前端缺省显示"—"不破坏

---

## 前端衔接（分工指令 B5）

前端消费 `themeBreakdown[].prev`：`prev` 存在 → 比较 `count` 显示趋势（上升绿 ↑ / 下降红 ↓）；`null` → 显示"—"。一行消费，无需返工。

*实施报告 · 后端唯一改动完成，前端 B5 可随时接入。*
