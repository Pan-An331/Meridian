# V3 前端实施简报（2026-08-02 晚 · 前端先行步完成）

> 依据：《V3实施-开发分工指令.md》第 1 步 · 交付物：预览 UI 已验收 → 本简报为「把新内容增添到真实项目」的成果
> 原则：**保留原项目文字大小/日历尺寸/交互，只增添 V3 新内容**，字段契约对齐 V3 §4.4

---

## 1. 改了什么

### 新增文件
| 文件 | 内容 |
|------|------|
| `src/lib/plan/colors.ts`（改造） | **领域 7 类新配色**（learning #3B82F6 提亮 / practice #7C3AED / external #92400E 深琥珀 / health #16A34A 绿 / life #CA8A04 / course #475569 / other #CBD5E1）；**新增 THEMES 表**（考研橙 #F97316 / 竞赛粉 #DB2777 / 身材青绿 #0D9488）+ `resolveTheme(tags,title)`（theme:* 前缀 → 标题关键词 → null 不强猜）+ `themeColor` 兜底；`normalizeCategory` competition→practice **显示迁移**（V3 D8，后端迁移前旧数据也显示为专业实践） |
| `src/components/task/ThemeBadge.tsx` | V3 主题徽章（色点+文字，对比度 ≥4.5:1；mini 态只色点），Plan/Inbox/Review/档案/搜索 全站复用 |
| `src/components/task/ArchiveProvider.tsx` | 全局档案面板 Context（挂 DashboardShell，任意页面可开） |
| `src/components/task/TaskArchivePanel.tsx` | **任务档案面板 560px 五区块**：①身份（标题/领域 7 类 chips/主题 chips+自定义选色/预估，可保存）②结构（归属链+📍去 Project）③时间（截止+排期+📅去 Plan）④执行（只读：子任务/已投入）⑤AI（折叠只读 + 红线标注） |
| `src/components/search/GlobalSearch.tsx` | **全局搜索**：优先 `GET /api/tasks/search?q=`，后端未就绪 404 → 本地拉 /api/tasks 过滤兜底；⌘K 聚焦；点击结果 → 打开档案面板 |
| `tests/v3-colors.test.ts` | resolveTheme/resolveDomain/normalizeCategory/DOMAINS 单测 |

### 改造文件
| 文件 | 改动 |
|------|------|
| `src/app/(dashboard)/plan/page.tsx` | 周历任务块标题旁**主题徽章**；分类行+图例行补**主题徽章/图例**；**重叠排期轨道分列**（assignLanes，重叠任务并排不遮挡） |
| `src/app/(dashboard)/inbox/page.tsx` | EditPanel 分类下拉→**7 类 chips（专业实践（含竞赛）小字）**；新增**主题 chips（考研/竞赛/身材+自定义选色+无主题）**；确认卡片显示主题徽章 |
| `src/app/(dashboard)/review/page.tsx` | 新增**指标卡 4 格**（周完成率用现有 avgCompletionRate，采纳率/堆积率/打卡保持率占位待 stats 阶段 C）+ **主题投入区块**（resolveTheme 聚合真实完成任务的专注时长） |
| `src/app/(dashboard)/projects/page.tsx` | 分类色改用 normalizeCategory（competition 旧数据也显示专业实践紫） |
| `src/components/DashboardShell.tsx` | 包一层 ArchiveProvider（档案面板全局可用） |
| `src/components/nav/topbar.tsx` / `sidebar.tsx` | 两种导航形态都挂全局搜索框 |
| `src/types/inbox.ts` | InboxDraftItem + `theme?: string \| null` |

### 未动（遵守「不改我的」）
- 各页面文字字号、周历行高（68px/h）、布局、现有 TaskDetailPopover 快速面板、拖拽/排期/勾选交互全部保留
- 后端文件零改动（colors.ts 属 V3 B1 服务层，前端先行一并落地，后端阶段直接复用）

---

## 2. 验证结果

| 验证 | 结果 |
|------|------|
| `tsc --noEmit` | ✅ 通过（修 2 处 DOMAINS border/color 字段名） |
| `next build`（.next-prod） | ✅ 通过，全部页面/API 编译成功 |
| `vitest run tests/` | ✅ 50/50 通过（新增 12 个 V3 单测 + 修 navigation 过期断言） |
| dev server 冒烟 | ✅ login/inbox/plan/review/today/projects 全部 200 |

---

## 3. 与后端（阶段 A-D）的衔接点

| 前端现状 | 后端就绪后 |
|---------|-----------|
| 主题：编辑面板/档案乐观 UI（PUT 白名单暂无 theme） | 阶段 C3 PUT +theme 白名单 → 自动持久化，前端无需改 |
| 搜索：`/api/tasks/search` 404 时本地过滤兜底 | 阶段 C1 新增 search API → 自动切真实检索（含归属链 contains） |
| 指标卡：采纳率/堆积率/打卡保持率显示"—"占位 | 阶段 C8 stats +themeBreakdown + 指标数据 → 自动填充 |
| 周历/档案主题：resolveTheme 从 tags/标题推断 | 阶段 A 迁移落库 theme 字段 + C5/C6 API 返回 theme → 直接读取 |
| DOMAINS 已 7 类，normalizeCategory 显示迁移 | 阶段 A1 数据迁移后旧 competition 记录物理归 practice |

**建议后端工程师开工时**：schema 加 `theme String?` 后，前端 colors.ts 的 THEMES/resolveTheme/themeColor 直接复用；档案面板 GET/PUT 补 theme 字段即完成对接。

---

*阶段报告 · 前端先行步完成。遗留：Project 树节点主题徽章（V3 §5.4 可选）、Today Focus Card 主题小徽章（克制，待 Today 轮）。*

---

## 追加：前端对接真实数据（分工指令第 3 步 · 2026-08-03 01:1x）

> 依据《V3后端实施报告-2026-08-03》交接表，5 项对接全部完成（tsc ✅ / vitest 59/59 ✅ / next build ✅）

| 前端现状 | 对接动作（已完成） |
|---------|------------------|
| TaskArchivePanel.save() 未发 theme | ✅ body 补 `...(theme ? { theme } : { theme: null })`，PUT 白名单 C3 已支持，真实持久化；提示文案更新为"已回流 AgentFeedback" |
| 档案面板归属链前端自己拼 | ✅ 直读后端 `ancestors`（C7 返回标题数组），删掉本地 /api/tasks 全量拉取 |
| 档案面板 AI 区 | ✅ 直读 `aiFields`（C7）；时间区新增 `schedules` 排期列表 + `accumStats` 续排天数 |
| Review ThemeInvestment 按专注时长 | ✅ 改消费 `stats.themeBreakdown`（D17 按任务数：theme/count/percent/label 主攻\|待加强） |
| Review MetricCards 3 格占位"—" | ✅ 消费 `stats.metrics.{completionRate,adoptionRate,backlogRate,checkinKeepRate}`（stats 类型 +可选字段） |
| GlobalSearch 用 resolveTheme 推断 | ✅ 直读 `h.theme`（C1 返回落库主题，fallback 场景仍推断） |
| Plan 周历/图例 resolveTheme 推断 | ✅ 直读 `t.theme`（C6 week-calendar 已返回 theme/themeColor，无则推断兜底） |

**前端第 3 步完成。** 至此 V3 前端先行 → 后端 A-D → 前端对接 全链路闭环。遗留：Project 树节点主题徽章（可选）、Today Focus Card 主题小徽章（克制，可选）。
