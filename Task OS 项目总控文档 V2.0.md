# Meridian（Task OS）项目总控文档 V2.1（产品宪法）

> 版本：V2.1 | 日期：2026-08-04（V2.0 基础上全面状态更新）
> 前版：`G:\Agent_Project\Task_Manage_Sys\Task OS 项目总控文档 V1.0.md`（理念愿景版，已过时）
> 性质：**产品宪法 / 开发 SSOT（单一事实源）**。任何开发 Agent（含 LLM + skill）修改代码前必读本文。
> 品牌：产品已定名 **Meridian · 子午**（slogan「你的时间，自有中轴」），"Task OS" 为历史工程名
> 配套规范：`docs/任务信息架构规范-V3.md`（信息架构与开发契约的完整细节）

---

## 0. 文档使用说明（IMPORTANT）

1. **本文档是产品宪法**：定义"是什么、为什么、不可破坏的原则、当前状态、怎么协作"，不写"哪个文件怎么改"（那由各规范文档和审查报告负责）
2. **开发必读顺序**：本文档 → `docs/任务信息架构规范-V3.md` → `docs/API接口清单.md` → `AGENTS.md`（Next.js 版本警告）
3. **SSOT 原则**：任何字段/概念/规则只在一个地方定义真值。信息架构规范 V3 是任务信息的唯一事实源；本文引用而不重复
4. **决策记录**：所有产品决策记录在本文 §7 与 V3 附录，禁止开发中自行发明语义
5. **目录导航**：项目结构/交接说明见根目录 `README.md`（整理于 2026-08-04，项目可整体移动）

---

## 1. 产品宪法

### 1.1 一句话定义

> Meridian 是一个把大模型理解能力转化为可执行时间系统的**个人成长网站**。
> 用户负责表达，AI 负责整理、规划、执行陪伴与复盘。

### 1.2 产品定位（决策 D1）

- **不是** Todo List（只能回答"我有什么事情"）
- **不是** 日历软件（只能记录已确定安排）
- **不是** AI 聊天机器人（对话结束即结束）
- **是** 个人成长操作系统：服务 **"成长闭环"（输入 → 规划 → 执行 → 复盘 → 更懂自己）**

**定位证据**：领域 7 类中 5 类学业相关（课程/学习/实践/竞赛/外部），用户行为以考研/电赛/毕设为主 → 核心用户是学生/备考生，产品价值是"帮用户变强"而非"管理杂事"。

**定位约束**：一切功能决策对照"是否服务成长闭环"。与成长闭环无关的功能（如泛用任务模板）默认不做。

### 1.3 核心理念（不可破坏）

> **帮用户过滤未来，而不是堆积未来。**

- 用户打开 Today 不应该思考"我今天有什么"，而应该直接知道"我现在该做什么"
- 任务信息越多，越要克制展示（卡片三层规则，见 §3.3）
- 任何新增展示信息必须通过"信息四关"：能改变行为吗？此刻需要吗？重复吗？有行动指向吗？

### 1.4 北极星与健康指标（决策 D2）

| 类型 | 指标 | 含义 |
|------|------|------|
| 北极星 | 周任务完成率 | 完成任务数 ÷ 应执行任务数（积累型按打卡率） |
| 驱动 | 今日路线采纳率 | AI 排期被确认/拖拽的比例 |
| 驱动 | Inbox 解析确认率 | 用户从 Inbox 确认创建的比例 |
| 驱动 | 积累型 7 天打卡保持率 | 连续坚持质量 |
| 健康 | 任务堆积率 | >7 天未处理任务占比（**过滤失败信号**） |
| 健康 | 周回归率 | 上周用户本周是否继续使用 |
| 反指标 | AI 建议修改率 | 用户常改 AI 排期 = 规则引擎该调 |

落地：Review 页面增加「指标卡」区块（数据来自 stats API，零新后端）。

### 1.5 五条产品红线

| # | 红线 | 内容 |
|---|------|------|
| R1 | 提醒只服务承诺兑现（决策 D4） | 只提醒"承诺过的事"（积累打卡/截止前 1 天），必须可一键全关；**禁止泛通知**（新功能/周报推送）。注：产品落地形态是网站，浏览器通知权限受限，天然克制 |
| R2 | 卡片三层一致性（决策 D6） | 所有任务卡片（Today/Plan/Project/收集箱/搜索结果）统一显示：身份 → 时间 → 状态，其余进详情。禁止任何页面自创卡片样式 |
| R3 | AI 结论进主流程、推理进设置（决策 D7） | 主流程 AI 只输出"结论 + 可应用动作"（如 Review「下周可以试试」）；Trust Score/记忆列表/决策日志只在 Settings |
| R4 | 数据可迁移（决策 D5） | 导出 JSON 必须带 `schemaVersion` 字段 + 迁移映射表；数据是用户资产 |
| R5 | 用户最终控制权 | AI 只建议不强制；AI 管理过程，用户管理方向 |

---

## 2. 核心循环与页面链

### 2.1 核心循环

```
用户输入 → Inbox(理解) → Plan(规划) → Today(执行) → 反馈/采集 → Review(复盘) → AI 更懂用户 → 优化下一轮
```

### 2.2 四页面链（+1 个整理页）· 导航中文化（决策 D18）

| 页面 | 导航名（中/英） | 职责 | 一句话 |
|------|----------------|------|--------|
| Inbox | 收纳 / Inbox | AI 理解入口 | "告诉系统发生了什么"（非创建任务） |
| Plan | 蓝图 / Plan | 时间规划中心 | 未来怎么安排（时间轴甘特 + 拖拽） |
| Today | 此刻 / Today | 执行驾驶舱 | 现在做什么（Focus Card + 路线 + AI 助手） |
| Review | 复盘 / Review | 复盘学习中心 | 过去怎么样 + 行为洞察 + 建议 |
| Project | 项目 / Projects | 项目整理 | 任务的层级/清单/归属的树形管理 |

**页面与信息归属**（详见 V3 §1）：身份层在 Inbox 定、结构层在 Project 管、时间层在 Plan 管、执行层在 Today 产生、AI 增强层全自动。

---

## 3. 任务信息架构（SSOT 摘要 → 详见 V3）

### 3.1 五层信息模型

| 层 | 管理页面 | 谁来填 | 核心字段 |
|----|---------|--------|---------|
| 身份层 | Inbox | AI 解析 + 人工确认（唯一确认点） | title/category/theme/taskType/预估/source |
| 结构层 | Project | 人工（AI 不碰） | parentId/level/accumulate/children/备注/排序 |
| 时间层 | Plan | 人工 + AI 建议（必须确认） | Schedule 时间块/deadline/续排 |
| 执行层 | Today | 执行时自动产生 | status/TimeLog/勾选/completedAt |
| AI 增强层 | 无 | AI 全自动（不进卡片） | complexity/riskLevel/dependencies/scheduleAdvice |

**核心原则：一个信息只有一个"家"，在哪生，就在哪管。** 任何字段只有一个编辑入口（收银台模式：档案面板读聚合、编辑跳转原页面）。

### 3.2 四维分类模型

| 维度 | 回答 | 数量 | 决定什么 |
|------|------|------|---------|
| 形态 | 怎么显示 | 固定 4 种（计时/清单/学习/积累），**不落库、前端推断** | 卡片长相与操作 |
| 领域 | 属于什么生活 | **7 类封顶**：课程/学习/实践/健康/生活/外部/未分类 | 颜色基调 |
| 主题 | 为了什么目标 | 自由扩展（`Task.theme` 字段） | 角标色（竞赛红/考研橙…） |
| 项目 | 在哪棵树 | 任意深度（Project 树） | 归属链 |

### 3.3 卡片三层显示规则

**身份 → 时间 → 状态**，其余进详情。"卡片是提示牌，详情才是档案室"。

---

## 4. 技术开发红线（不可违反）

1. **Schedule 是唯一时间数据源**：任何时间修改必须经 Schedule Service（$transaction）；Task 上无时间字段（V2 已删除 startTime/endTime）
2. **Task ≠ Schedule**：任务不改时间，时间由 Schedule 管理
3. **形态不落库**：卡片形态由数据推导（有排期→计时 / 有子任务→清单 / accumulate→积累 / 否则→学习），禁止新增形态字段
4. **单一编辑入口**：同一字段禁止两个页面可写（档案面板是"读聚合"不是"写旁路"）
5. **零 AI 可用底线**：未配置 AI 的用户能完整使用全部核心功能；Today 规则引擎（today-decide/pause-advisor/execution-advisor）不可删
6. **LLM 不出 taskId**：LLM 只出 keyword，后端 Resolver 解析
7. **AI 不直接写结构/时间**：AI 建树/排期必须经用户确认（Inbox 表单 / apply-decision）
8. **分类字段为准**：Task.category 是事实源；tags 只存用户自由标签（系统语义全部字段化，无 domain:/theme:/important: 前缀）

---

## 5. 项目当前状态（2026-08-04）

### 5.1 已完成（全部验收通过）

**信息架构与核心能力**
- **V3 信息架构全链路**：theme 机制（schema+迁移+解析+API）、档案面板（五层聚合）、全局搜索、Review 指标卡 + 主题投入（含周环比）、领域反馈回流、5 死字段清理、死路由清理
- **Focus Card V2**：动机行（purpose+父级继承）、出发/回来确认/补记时长、忘记确认提示条、暂停恢复（原因采集）、固定时间型惰性结算、五类型卡片、memo 黄执行清单
- **五页面布局重排**：Today 弹性布局（760/880 双态）、Review 两栏化（左证据右仪表）、Plan 折叠条（本周截止/续排/健康度）

**品牌与产品化（2026-08-03/04）**
- **品牌定名 Meridian · 子午**：Logo C 方案（断口双点）、品牌色子夜蓝 #1E3A8A + 正午金 #F59E0B、登录页 v4（slogan 三层）、导航中英切换（默认中文：收纳/蓝图/此刻/复盘/项目）
- **Project 页优化**：树行瘦身（分组头/圆点/连接线）、派生色（themeColor）、待整理池 AI 挂入建议、★ 清单开关持久化、档案面板统一
- **收尾批次**：续排按钮（Plan 续排条 + Today 次级按钮）、忘记确认提示条、左栏项目阶段直读、mini-cal 当月小日历（monthDates）
- **全站移动端适配**：375px 实测、周历移动端模式、触控优化

**工程基础**
- **数据库迁移 SQLite → PostgreSQL**：20 表 1717 行迁移完成，本地便携 PG + 迁移/验证脚本
- **测试体系**：vitest 95/95（11 文件），tsc 零错，next build 通过
- **PWA 就绪**：manifest/sw.js/图标（public/）
- **目录整理 + 交接准备（2026-08-04）**：docs 62→10、UI示例 21→4 定稿、归档目录体系、README/AGENTS 重写、用户使用手册（docs/用户使用手册.md）、git commit c38b75a

### 5.2 待办（按优先级）

| 优先级 | 事项 | 依据 |
|--------|------|------|
| P0 | **云端上线（用户账号操作，约 30 分钟）**：GitHub 推送 → Neon 建库 → Vercel 部署（按 `docs/Meridian-云端部署-用户操作指南-2026-08-04.md`） | 部署方案 |
| P1 | 开新账号真实使用，积累数据后验证指标（周完成率/堆积率基线） | D2 |
| P2 | Next 路线图（见 `docs/项目开发地图.html`）：打卡提醒克制版 / 语音输入（需 PoC）/ AI 周报主动性 / 技术债（any 类型） | 路线图 |
| P3 | 小遗留：currentTask 返回 parentId（现按名称链匹配）、Review 主题趋势消费 prev（前端已兼容 null） | 收尾批次 |

---

## 6. 开发协作契约（给其他 LLM / skill）

任何新开发 Agent 开工前必须：

1. **读本文档**（产品宪法）→ 确认定位、红线、当前状态
2. **读 `docs/任务信息架构规范-V3.md`**（信息 SSOT）→ 字段定义、分类、API、前端规则
3. **读 `docs/API接口清单.md`** → 接口现状，改接口前确认消费方
4. **读 `AGENTS.md`** → 本项目 Next.js 16 有破坏性变更，先读 `node_modules/next/dist/docs/`
5. **改 schema 后**：`prisma db push` + `prisma generate` 双跑（dev.db 锁需先杀 dev server）
6. **build 前置**：`export NODE_OPTIONS=""`；清 `.next/.next-prod` 缓存；distDir 是 `.next-prod`
7. **命名规范**：taskType=「计划状态」；卡片形态=「卡片形态」；分类=「领域」；不发明新语义
8. **写文档**：任何新决策更新到 V3 附录决策记录，不散落

---

## 7. 决策记录（含 7 个产品问题）

| # | 决策 | 内容 | 状态 |
|---|------|------|------|
| D1 | 产品定位 | 个人成长系统（非泛用 GTD），服务成长闭环 | ✅ 定稿 |
| D2 | 北极星指标 | 周完成率/采纳率/堆积率 + Review 指标卡 | ✅ 本期实施 |
| D3 | 主题复盘 | Review「主题投入」分析（themeBreakdown） | ✅ 本期实施 |
| D4 | 提醒红线 | 只服务承诺兑现 + 可一键关；网站形态天然克制 | ✅ 定稿 |
| D5 | 数据主权 | 导出加 schemaVersion + 迁移映射 | ✅ 定稿 |
| D6 | 卡片三层一致 | 所有卡片身份→时间→状态，禁止自创 | ✅ 定稿 |
| D7 | AI 透明化边界 | 结论进主流程、推理/信任分进 Settings | ✅ 定稿 |
| D8 | 领域 7 类封顶 | competition 并入实践 + 主题竞赛；「考研」不再硬塞 learning | ✅ V3 |
| D9 | theme 独立字段 + 索引 | `Task.theme` + `@@index([userId, theme])` | ✅ V3 |
| D10 | 分类选择器提示 | 「专业实践」旁加"（含竞赛）" | ✅ V3 |
| D11 | 主题预设 chips | ✅ 2026-08-02 预览验收改为 **3 个：考研/竞赛/身材** + 自定义选色输名（原 5 个方案作废，见 V3 D15） |
| D12 | 字段清理 | 删 temperature/startTime/endTime/Task 级 cognitiveLoad/schedulingHint | ✅ V3 |
| D15-D17 | 预览验收补充决策 | 主题预设 3 个 / 领域重配色+暖色纪律 / 主题投入按任务数（细节见 V3 §7） | ✅ 2026-08-02 |
| D18 | 导航中文化 | 收纳/蓝图/此刻/复盘/项目 + 中英切换设置（默认中文） | ✅ 2026-08-04 |
| D19 | 品牌定名 | **Meridian · 子午**（slogan「你的时间，自有中轴」），Logo C 断口双点，子夜蓝+正午金 | ✅ 2026-08-04 |
| D20 | Focus Card V2 | 动机行（purpose）/ 出发-回来确认 / 惰性结算 / 忘记确认提示 / 暂停恢复 | ✅ 2026-08-04 |
| D21 | 数据库迁移 | SQLite → PostgreSQL（20 表 1717 行），Vercel+Neon 免费部署 | ✅ 2026-08-04 |

---

*本文档替代 V1.0（理念愿景版），V2.1（2026-08-04）更新全部当前状态与决策。所有「已完成/待开发」状态以本文 §5 为准；信息架构细节以 V3 为准；API 细节以 API 接口清单为准；目录导航以 README.md 为准。*
