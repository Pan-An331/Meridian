# 2026-08-06 工作日志

## 迭代批次 P1-6/P1-4/P1-11/P1-2 前端实施（16:5x-17:2x）
- **commit f29be8e 已推送 main（Vercel 自动部署）**，依据《迭代交接指令-2026-08-06.md》+ 产品改进清单 v2，只做 4 项
- **A · P1-6 待整理池可拖拽**（projects/page.tsx + globals.css）：池项补 draggable + onDragStart（dragSource="pool"），复用现有 onRowDrop「挂为子级」分支；CSS 加 cursor:grab + hover 金色提示 + dragging 半透明
- **B · P1-4 Inbox 输入框可取消**（inbox/page.tsx InputCanvas）：closeInput() = 清空+失焦+高度复位；Escape 关闭（不再只清空）、footer「✕ 取消」按钮、blur 空值收起（有内容保留防误丢，避免点 AI 整理按钮时 blur 清空）
- **C · P1-11 Focus Card 清单可新增项**（FocusCardV2.tsx + today/page.tsx）：Props 加 onItemAdd；MemoList 标题行加号 → inline 输入（Enter 提交/Esc 取消/blur 收起）；今日页 addChildItem 乐观更新 → POST /api/tasks {parentId, level:"task"}（现有 API 复用）→ load() 落库
- **D · P1-2 0 分钟显示**（FocusCardV2.tsx）：plannedLabel/elapsedLabel helper；无排期 →「待排期」灰字；未开始 0 用时 →「—」（going 保留 0 分钟）；三处消费（左栏元信息/总耗时/E 行）+ D 区「已记」；「空闲」grep 确认只在 plan/page.tsx:311 + decision route，未误入 Today
- **验证**：tsc 零错误 / 95/95 测试零回归 / next build 通过
- **环境坑（重要经验）**：本环境 NODE_OPTIONS 注入 genie-safe-delete shim（--require genie-safe-delete.cjs），会把 Node 内部文件删除转向回收站且 fail-closed → `prisma generate` 的 safe-delete 必失败（genie-trash 在 Git Bash 路径解析坏）。**绕过：NODE_OPTIONS= npx prisma generate / next build**；且 NODE_OPTIONS 里 --use-system-ca 也导致 next build worker 报错，构建一律 `NODE_OPTIONS=` 前缀
- 简报：docs/archive/迭代批次-前端实施简报-2026-08-06.md（已归档）
- 遗留：mustDo 兜底卡新增清单项无乐观显示（无 currentTask 数据源）；「＋备注」仍纯展示；migrate-run2/3.log 未跟踪未提交

## 产品反馈梳理轮（17:26-17:5x）——只梳理未改码
- **输出**：docs/产品反馈问题清单与方案-2026-08-06.md（13 项反馈 → 8 bug + 5 决策 + 4 视觉）
- **核实的核心 bug**：① Plan「调整时间」UTC 泄漏——week-calendar 返回 toISOString() UTC 串，ScheduleModalInner 直接 slice(0,16) 进 datetime-local（差 8 小时）② 任务卡弹窗 top=innerHeight-420 硬编码致底部溢出不可滚 ③ **子任务全变"想法"**——POST /api/tasks 的 VALID_TYPES 不含 "task" fallback 成 inbox，且 week-calendar plannedTasks 未排除子任务 → 子任务进收集箱（level 与 taskType 两维度混淆）④ 跨凌晨任务未展开时块超 timeaxis 被裁剪（totalHours=TH+midnight?4:0）⑤ 主题「确定」不保存（inbox EditPanel 需两步）⑥ 备注装饰品（P1-11 遗留）⑦ 今日路线无提前执行（P1-1）⑧ Inbox 无类型选择（P1-5）
- **决策建议**：想法≠不紧急（时间维度 vs 重要性维度错位）→ 推荐文案「想法」→「待安排」+ Inbox 三轻量动作（加子任务/设每天/设有截止）；子任务继承父级 taskType + 想法列表排除子任务；Today 全宽推翻弹性双态（D-新3）；「待安排」命名待用户确认
- 待用户拍板后进批次 1（B2/B3/B6/B1/B4）→ 批次 2（P1-1/P1-9/P1-5）→ 批次 3（视觉）

## 迭代批次二全量实施（17:55-18:2x）commit c660184 已推送
- **B9 层级 P0**：move 路由拖拽挂载按父级推导 level（无父=project/父project=phase/父phase|task=task；换序不动）
- **B2**：ScheduleModalInner initialStart 转本地（原 slice UTC 串差 8h）；**B3** 弹窗 top 按实际卡高 min(70vh,520) + overflow-y-auto；**B6** 未展开凌晨跨夜块截断 + 「⋯ 跨夜」
- **B1** 删备注装饰品；**B4** POST /api/tasks 子任务继承父级 taskType + week-calendar 收集箱排除 parentId/容器
- **三态派生态**：src/lib/task/time-state.ts（deriveTimeState：时间块/截止日/事项），替换 7 处「想法」；taskType 降为创建意图
- **B7 主题落库**：Task.themeColor String?（JSON）+ **db push 已同步 Neon**（唤醒后成功）；POST/PUT/GET + tree/周历落库色优先；档案/Inbox 自定义「确定即保存」（theme+themeColor 一次）；ThemeBadge 加 color prop；nodeTheme 兼容 pcolor/落库色
- **B5** 今日路线点击切换 Focus Card（routeSel 前置卡）+ hover 提前执行；**B10** apply-decision 手动调整后 source→user + AI 徽章弱化灰字
- **视觉**：pt-row lvl1/2/3 padding-left 16/28/40（文字真实右移，竖线不再压字）；字号 12.5→13/13.5→14；**Today 全宽**（删弹性双态，max-w-1080）
- **实际用时补记**：新 API POST /api/tasks/[id]/time-log（TimeLog type=manual）+ 档案面板「＋补记」
- **B8** Inbox SimpleCard 动作行（加子任务/设每天/设有截止）+ 重要性三档；EditPanel 加重要性
- 验证：tsc 0 / 95/95 / next build 过；简报 docs/archive/迭代批次二-全量实施简报-2026-08-06.md
- 遗留：P1-12 Focus Card 放大、P2-7/8 其余字号细节待下一批次；migrate-run2/3.log 未跟踪

## 决策轮 v2（17:34-17:4x）——用户拍板 + 重大架构洞察
- **已拍板**：①「＋备注」装饰品删除（不做备注功能）② B5 今日路线做成点击切换 Focus Card + hover「提前执行」③ 决策 2 同意（子任务继承父级 taskType + 想法列表排除子任务）
- **自定义主题根因实锤**：Task 表只有 theme 字符串，**themeColor 无落库路径** → `themeColor("自定义名")` 查 THEMES 预设表失败返回灰兜底 → 用户"确定后无变化"= 颜色永远丢失（inbox EditPanel:261-267 / TaskArchivePanel:90,193 都只存名字）。方案 A 推荐：Task.themeColor JSON 落库 + 确定即保存（P1-9）；B 备选：名字 hash 定色（不推荐）
- **核心架构洞察（用户提出，采纳）**："想法/未排期是**状态**不是**分类**"——taskType 静态字段把状态当分类存，排期后仍显示"想法"（plan:636 实锤）。**新方案：时间状态派生态**——`deriveTimeState(task)` 由 Schedule/deadline 推导三态：已排期/截止日/待安排；替换全部 7 处「想法」标签（inbox TYPE_LABEL/plan 收集箱+详情/GlobalSearch/档案面板）；taskType 降级为"创建意图"（阶段 2 再评估退役）；Inbox 类型入口改为时间状态动作（加子任务/设每天/设有截止）
- 待拍板：「待安排」vs「未排期」命名；主题方案 A/B
- 文档 v2：docs/产品反馈问题清单与方案-2026-08-06.md（拍板进度 + 派生态方案）

## 决策轮 v3（17:42-17:5x）——重要性维度澄清
- 用户追问"小事/不影响大局"的定位 → **澄清两个维度正交**：小事/大事 = importance（重要性，Task 表已有 1-5 字段），待安排/已排期 = 时间状态（派生态）；**核实 importance 全站 UI 零暴露**（只有接口/种子引用）→ 用户没工具表达"小事"只能借用类型标签，别扭感根源
- **方案补充**：Inbox 确认表单加重要性三档（低/中/高）+ 档案面板可改 + 低价值任务弱化显示（配合"过滤未来"理念）；零模型改动（复用 importance）
- 画了 2×2 象限图（时间状态 × 重要性）给用户看
- 待拍板 +1：重要性三档是否随批次 2 做

## 决策轮 v4（17:51-18:0x）——P0 层级 bug 实锤 + 命名定稿
- **B9 P0 实锤**：POST /api/projects/move 只更新 parentId **不更新 level** → 拖拽设置层级后刷新，树接口按旧 level 重建 → "task 类型没了"。修复：移动按新父级推导重算 level（无父=project/父是project=phase/父是phase或task=task），事务内更新；换序不动 level。**用户强调 level 语义 = 哪些任务可被安排（可安排性）**，架构级
- **B10 AI 徽章失真**：task.source 创建时定 "ai"，手动拖/改时间只动 Schedule → 仍显示 AI。修复：手动调整后 source→user + 徽章弱化
- **三态命名定稿（用户钦定）**：截止日 / 时间块 / **事项**（未排期任务的性质名）——用户判定"待安排/已排期"是状态词不像分类；「事项」可与截止日/时间块并列比较；显示仍动态推导（排期后变时间块）
- Project 视觉用户点名优先：C1 字号 / C2 层级间距 / C3 竖线与文字重合（.pt-indent 压字）
- 文档 v4：docs/产品反馈问题清单与方案-2026-08-06.md；唯一待拍板：重要性三档（低/中/高）是否随批次 2 做
